#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mean-cli/bin/mean-list" "$@"
  ret=$?
else 
  node  "$basedir/../mean-cli/bin/mean-list" "$@"
  ret=$?
fi
exit $ret
