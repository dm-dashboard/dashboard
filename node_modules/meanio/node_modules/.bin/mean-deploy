#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mean-cli/bin/mean-deploy" "$@"
  ret=$?
else 
  node  "$basedir/../mean-cli/bin/mean-deploy" "$@"
  ret=$?
fi
exit $ret
