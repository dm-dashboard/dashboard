#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mean-cli/bin/mean-logout" "$@"
  ret=$?
else 
  node  "$basedir/../mean-cli/bin/mean-logout" "$@"
  ret=$?
fi
exit $ret
